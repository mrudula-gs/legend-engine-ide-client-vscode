I was able to recreate the issue withthis query
let result1 = meta::pure::executionPlan::executionPlan(|Holdable.all(%2018-02-28)->project([p|<span>p.person.roadVehicles->subType(@Car).description, p|</span>p.person.roadVehicles->subType(@Bicycle).description], ['descC', 'descB']), assocMapping3, meta::external::store::relational::tests::testRuntime(), meta::relational::extension::relationalExtensions());
// This test projects multiple subType properties through a chained navigation // (Holdable -> Person -> roadVehicles) which is closer to the original issue // with SwapAttributes where multiple leg types (InterestLeg, FeeLeg, etc.) caused duplicates
now before my fix sql was :
select "unionalias_0"."Carc_Description___SQLNULL__" as "descC", "unionalias_0"."SQLNULL___Bicycleb_Description" as "descB" from Holdable as "root" left outer join Person as "person_0" on ("person_0".ID = "root".ID) left outer join (select "root".c_PersonID as c_PersonID_0, null as b_PersonID_1, "root".c_Description as "Carc_Description___SQLNULL", null as "SQLNULL___Bicycleb_Description" from Car as "root" union all select null as c_PersonID_0, "root".b_PersonID as b_PersonID_1, null as "Carc_Description___SQLNULL", "root".b_Description as "__SQLNULL___Bicycleb_Description" from Bicycle as "root") as "unionalias_0" on ("person_0".ID = "unionalias_0".c_PersonID_0 or "person_0".ID = "unionalias_0".b_PersonID_1)
and after fix it is,
select "car_0".c_Description as "descC", "bicycle_0".b_Description as "descB" from Holdable as "root" left outer join Person as "person_0" on ("person_0".ID = "root".ID) left outer join Car as "car_0" on ("person_0".ID = "car_0".c_PersonID) left outer join Bicycle as "bicycle_0" on ("person_0".ID = "bicycle_0".b_PersonID)
